/**
 * Expected User Object Structure:
 * {
 *   id: "uuid",            // Unique string identifier generated by generateId
 *   name: "John Doe",      // User's name
 *   email: "john@example.com", // User's email (unique identifier for login)
 *   password: "hashed-password", // Password hashed using bcryptjs (not plain-text)
 *   role: "organizer" | "attendee", // User's role determines event management privileges
 *   createdAt: "ISO date"  // Creation timestamp in ISO string format
 * }
 * 
 * Expected Event Object Structure:
 * {
 *   id: "uuid",            // Unique string identifier generated by generateId
 *   title: "Node.js Workshop", // Event title
 *   date: "2026-08-20",    // Event date (YYYY-MM-DD)
 *   time: "18:00",         // Event time (HH:MM)
 *   description: "Backend development workshop", // Event details
 *   organizerId: "user-uuid", // User ID of the event organizer (User with role "organizer")
 *   participants: [],      // Array of User IDs (attendees) registered for the event
 *   createdAt: "ISO date", // Creation timestamp in ISO string format
 *   updatedAt: "ISO date"  // Last update timestamp in ISO string format
 * }
 */

// In-memory collection for Users
const users = [];

// In-memory collection for Events
const events = [];

module.exports = {
  users,
  events
};
